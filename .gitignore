# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Model / data artifacts (regenerable; keep the repo light).
# Exceptions: report/out/ (the deliverable the README links) and the single run
# backing the README's results table (run.json provenance) are committed.
data/*.jsonl
data/*.parquet
data/runs/*
!data/runs/20260718-010457
*.faiss
.cache/

# Local model + secrets
.env
*.gguf

# OS / editor
.DS_Store
.idea/
.vscode/
