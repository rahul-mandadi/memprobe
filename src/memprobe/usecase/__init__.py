"""Swappable reference domain. Default: synthetic SaaS customer support (see support.py).

The domain is isolated here so the harness, memory, and eval layers never hard-code
support-specific assumptions — swap this module to retarget the whole lab (NOTES.md: domain is
swappable, architecture is not).
"""
